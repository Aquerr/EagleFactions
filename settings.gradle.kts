rootProject.name = "EagleFactions"
include(":EagleFactionsAPI")

pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven("https://maven.minecraftforge.net/")
    }
}